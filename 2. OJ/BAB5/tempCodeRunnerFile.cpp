    // memset(matrix1, 0, size * sizeof(matrix1[1][1]));
    // memset(matrix2, 0, size * sizeof(matrix2[1][1]));
    // memset(matrix3, 0, size * sizeof(matrix3[1][1]));
    // memset(matrix12, 0, size * sizeof(matrix12[1][1]));
    // memset(matrix123, 0, size * sizeof(matrix123[1][1]));
