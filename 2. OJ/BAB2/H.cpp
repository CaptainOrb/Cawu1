#include <stdio.h>

int main()
{
    double a, b, c, d, sum;
    scanf("%lf %lf %lf %lf", &a, &b, &c, &d);
    sum = 2*a + 4*b/2 + 6*c/3 + d;
    printf("%.2lf\n", sum);
    scanf("%lf %lf %lf %lf", &a, &b, &c, &d);
    sum = 2*a + 4*b/2 + 6*c/3 + d;
    printf("%.2lf\n", sum);
    scanf("%lf %lf %lf %lf", &a, &b, &c, &d);
    sum = 2*a + 4*b/2 + 6*c/3 + d;
    printf("%.2lf\n", sum);
}